
import 'package:flutter/material.dart';

class Viewguest extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
     return Scaffold(
      appBar: AppBar(
        title: Text(
            "Audience",
            style: TextStyle(fontSize: 25, fontWeight: FontWeight.bold),
          ),
      ),
      body: Container());
  }
}